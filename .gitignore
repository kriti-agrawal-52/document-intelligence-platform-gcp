# Dependencies
node_modules/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.env
.venv

# IDEs
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Terraform
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/terraform.tfstate*
terraform/tfplan*
terraform/backend-only-plan

# Build outputs
dist/
build/
.next/

# Environment files
.env.local
.env.development.local
.env.test.local
.env.production.local

# Scripts directory (utility scripts not needed in version control)
scripts/

# Documentation directory (guides and docs not needed in version control)
documentation/

# External Secrets configuration (not needed - using GitHub Secrets instead)
# We use GitHub Secrets for CI/CD deployment which is simpler, faster, and saves ~$1/month
# External Secrets would be useful for runtime secret rotation, but GitHub Secrets work perfectly for deployment-time injection
kubernetes/01-external-secrets.yaml

# Temporary files
*.tmp
*.temp
temp/

# Cache
.cache/
.npm/
.yarn/

# Coverage reports
coverage/
.nyc_output/

# Database
*.db
*.sqlite

# Docker
.dockerignore

# Local development
local/
dev/

# Secrets (double-check these are not committed)
terraform/terraform.tfvars
kubernetes/01-gcp-secrets.yaml

# MacOS
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*github-actions-key.json

# Security: Never commit these files
*.tfvars.secret
*secret*.json
*key*.json
*credentials*.json
.env*
!.env.example
*password*
*api*key*
*jwt*
*mysql*
sk-*
gcp-sa-key*

# Kubernetes secrets files (but not service files)
kubernetes/*secrets*.yaml
kubernetes/*-secrets.yaml
kubernetes/01-*secrets*.yaml
kubernetes/*credentials*.yaml
!kubernetes/*-service.yaml
!kubernetes/*-external-*.yaml

# Terraform sensitive files
terraform/*.tfvars
!terraform/*.tfvars.example
terraform/.terraform/
terraform/terraform.tfstate*
terraform/.terraform.lock.hcl

# Backup and temporary files
*.backup
*.tmp
*.bak
*~
